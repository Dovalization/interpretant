defaults:
  - corpus: s2orc
  - embedding: word2vec
  - alignment: procrustes
  - drift: cosine
  - _self_

data_dir: data
models_dir: models
output_dir: outputs/${now:%Y-%m-%d_%H-%M-%S}

decades:
  start: 1960
  end: 2020
  step: 10

vocab:
  min_count: 10
  max_vocab_size: 100000

seed: 42
