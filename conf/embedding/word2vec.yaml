# @package _global_.embedding
name: word2vec
vector_size: 300
window: 10
min_count: ${vocab.min_count}
workers: 4
epochs: 5
sg: 1
hs: 0
negative: 10
seed: ${seed}
model_dir: ${models_dir}/raw/${corpus.name}/word2vec
